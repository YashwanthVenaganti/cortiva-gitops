apiVersion: v1
kind: Service
metadata:
  name: cortiva-backend
  namespace: cortiva

spec:
  selector:
    app: cortiva-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: ClusterIP
