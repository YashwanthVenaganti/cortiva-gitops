apiVersion: v1
kind: Service

metadata:
  name: cortiva-service
  namespace: cortiva

spec:
  selector:
    app: cortiva-backend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: ClusterIP